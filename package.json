{
  "name": "gobus-web",
  "version": "1.0.0",
  "description": "A web client for the gobus project",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "http://pauline-mint/pauline/gobus-web.git"
  },
  "author": "pca",
  "license": "ISC",
  
  "devDependencies": {
    "http-server": "^0.9.0"
  },

  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a 0.0.0.0 -p 8001"
  }
}
